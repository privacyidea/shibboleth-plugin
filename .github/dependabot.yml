version: 2
updates:
  # Keep the GitHub Actions pinned in the workflows above up to date (low noise).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
